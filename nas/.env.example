# Werte aus Vercel → Storage → (Upstash-Speicher) → Quickstart / .env.local
# Nur den Nur-Lese-Schlüssel eintragen. Diese Datei als .env speichern,
# sie gehört nicht ins Repo.
KV_REST_API_URL=
KV_REST_API_READ_ONLY_TOKEN=
